import SwiftUI

public enum CalculatorPalette {
    public enum Background {
        public static let accent = Color.orange
        public static let primary = Color.gray
        public static let secondary = Color.secondary
    }
}
